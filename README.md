# staple-css
staple-css is a shared styling contract for apps—tokens, primitives, and accessible components. Semantic CSS variables standardize spacing, color, typography, and variants. Tokens are the API. Consistency by default, with deliberate escape hatches.
